Вывод:
<nil>
false
Существует пустой интерфейс nil, и nil конкретного типа.
Если интерфейсу присвоить nil без типа (например, return nil), то он будет истинно nil.
Если интерфейсу присвоить nil конкретного типа (например, var err *os.PathError = nil), то интерфейсное значение будет non-nil, потому что оно содержит информацию о типе.